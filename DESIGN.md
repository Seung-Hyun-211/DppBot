# DppBot 설계 문서

디스코드 음악봇을 **C++ 클라이언트(dpp)** + **Go 서버**의 2-프로세스 구조로 다시 만든다.
이 문서는 이전 구현(레거시)을 검토하며 찾은 문제들을 설계 규칙으로 못박아 둔 것으로, 앞으로의 구현은 이 문서를 기준으로 진행한다.

## 1. 전체 아키텍처

```
Discord ── (gateway/voice) ──> C++ 봇 클라이언트 (dpp)
                                     │
                                     │ HTTP, 127.0.0.1:8080 (로컬 전용)
                                     ▼
                                Go 서버
                                     │
                    ┌────────────────┼────────────────┐
                    ▼                ▼                 ▼
             YouTube Data API   yt-dlp + ffmpeg      MySQL
             (검색/메타데이터)   (다운로드/PCM 변환)   (곡 정보, 추천 목록)
```

- 클라이언트와 서버는 완전히 분리된 프로세스이며, 오직 로컬 HTTP로만 통신한다.
- 서버는 외부에 노출하지 않는다(기본 바인딩 `127.0.0.1`). 봇과 서버는 같은 호스트에서 실행된다고 가정한다.

## 2. 컴포넌트

### 2.1 C++ 봇 클라이언트 (`src/`)

**책임**: 디스코드 명령어 파싱, 길드별 음성 연결/재생 큐 관리, 서버 API 호출, 오디오 스트리밍.

| 타입 | 역할 |
|---|---|
| `DiscordBotClient` | 명령어 라우팅, 길드별 상태(`guildInfos`) 관리 |
| `GuildInfo` | 길드 하나의 음성 연결, 재생 큐, 오디오 스레드 |
| `VideoDbInfo` | 서버가 돌려준 곡 메타데이터 + 로컬 PCM 경로 |

**명령어**

| 명령어 | 동작 |
|---|---|
| `!j` / `!join` | 명령어를 입력한 유저의 음성 채널에 접속 |
| `!l` / `!leave` | 음성 채널 퇴장, 재생 상태 초기화 |
| `!p <검색어\|url>` / `!play` | 검색/다운로드 후 재생 큐에 추가 |
| `!f <검색어\|url>` / `!first` | 재생 큐 맨 앞에 추가 (바로 다음 재생) |
| `!s` / `!skip` | 현재 곡 스킵 |
| `!omakase [개수]` | DB에서 무작위 추천 곡을 큐에 추가 (기본 30) |
| `!list` | 현재 재생 중 + 다음 곡 목록 출력 |
| `!random` | 재생 큐 셔플 |
| `!remove <인덱스>` | 큐에서 특정 곡 제거 |
| `!delete [s]` | 현재 곡을 추천 대상에서 비활성화 (`s`를 붙이면 즉시 스킵까지) |
| `!repeat` | 현재 곡 반복 재생 토글 (곡이 끝까지 다 재생됐을 때만 적용, `!skip`은 반복 여부와 무관하게 항상 다음 곡으로) |
| `!clean [개수]` | 현재 채널의 최근 메시지 삭제 (기본 10, 최대 10 — 넘으면 삭제 없이 안내만) |

**자동 동작**

- **자동 퇴장**: 봇이 있는 음성 채널에 봇 외(다른 봇 제외)에 아무도 없는 상태가 10초 이상 지속되면 `!leave`와 같은 정리를 하고 마지막 명령 채널에 안내 메시지를 보낸다. 인원은 `on_voice_state_update` 이벤트로 유지하는 자체 자료구조로 세고, 2초마다 타이머가 확인한다.
- **자동 재접속**: 음성 연결이 5초 이상 죽어 있으면 마지막으로 접속했던 채널로 다시 접속을 시도한다(5초 간격 재시도).

**동시성 규칙**

- 길드별 상태(`GuildInfo`)는 전역 `guildInfosMutex`로, 재생 큐는 길드별 `listMutex`로 보호한다. 두 락을 동시에 오래 들고 있지 않는다(락 구간을 최소화).
- 길드마다 오디오 재생 스레드를 최대 1개만 둔다.
- **음성 연결을 끊거나 교체하기 전에는 반드시 오디오 스레드를 먼저 정지시킨다.** 순서를 반대로 하면(연결을 먼저 끊고 스레드를 나중에 멈추면) 오디오 스레드가 이미 해제된 `voiceclient`를 참조해 use-after-free가 날 수 있다 — 레거시의 join 처리에서 실제로 이 순서가 뒤집혀 있었다.
- 서버로 보내는 HTTP 요청(다운로드, 추천 조회 등)은 디스코드 이벤트 콜백 스레드를 블로킹하지 않는 워커 스레드/비동기 경로에서 수행한다. 콜백 스레드에서 동기 호출하면 응답이 오는 동안 다른 길드의 메시지 처리가 멈춘다.
- 길드 상태 구조체를 이동(move)할 때는 재생 큐, 현재 재생 곡, 스레드 핸들을 포함한 **모든 필드**를 명시적으로 옮긴다. 필드 하나라도 빠지면 조용히 데이터가 사라진다.
- **dpp 객체 포인터를 캐싱하지 않는다.** `voiceconn*`(음성 full reconnection 때 교체)과 `discord_client*`(게이트웨이 resume 때 복사·교체되고 이전 객체는 파괴)는 재연결 때 사라지므로, 안정적인 `shardId`만 저장하고 쓸 때마다 `bot.get_shard(shardId)` → `get_voice(guildId)`로 다시 조회한다.
- **재연결 중인 `discord_voice_client`에 `send_audio_raw`/`send_silence`를 호출하지 않는다.** dpp는 음성 웹소켓이 끊기면 1초 뒤 타이머 스레드에서 같은 객체 안의 Opus 인코더를 `cleanup()`으로 파괴하고 `setup()`으로 다시 만드는데, `send_audio_raw`는 그 인코더를 락 없이 쓴다. 호출 직전에 `is_connected() && is_ready() && !terminating`을 매번 확인하고(`IsVoiceClientSafeToSend`), 아니면 같은 프레임을 들고 기다린다(최대 20초). 스택 트레이스로 확인된 실제 크래시 원인이다.
- **dpp 캐시의 `guild->voice_members`를 반복 순회하지 않는다.** dpp 이벤트 스레드가 락 없이 수정한다. 인원 추적은 `on_voice_state_update`로 유지하는 자체 자료구조(자체 뮤텍스)를 쓰고, 캐시 접근은 세션당 스냅샷 한 번으로 제한한다. `guildInfosMutex`와 `voiceMutex`는 동시에 잡지 않는다.
- 외부 프로세스(curl 등)를 셸 문자열 조립 + `popen`으로 실행하지 않는다. HTTP 클라이언트 라이브러리를 직접 쓰거나, 프로세스 실행이 꼭 필요하면 인자 배열을 그대로 전달하는 API(셸을 거치지 않는 방식)를 쓴다.

### 2.2 Go 서버 (`server/`)

**책임**: 검색/다운로드 오케스트레이션, PCM 변환, DB 저장, 추천 목록 제공.

**요청 처리 파이프라인** (`/process` 기준, 한 요청은 아래 단계를 순서대로 한 번만 탄다)

```
입력 정규화 (url 파라미터 우선, 없으면 q)
  → start_radio 거부
  → 플레이리스트 판별 (list= 파라미터) → 맞으면 플레이리스트 파이프라인으로 위임하고 종료
  → videoID 결정 (URL 파싱 또는 YouTube 검색)
  → 메타데이터 조회 (duration 포함) → DB 저장
  → 로컬에 없으면 다운로드(yt-dlp) + 변환(ffmpeg) → 있으면 스킵
```

**리소스 관리 원칙**

- MySQL 커넥션 풀은 서버 시작 시 한 번만 연다. 요청마다 새로 열고 닫지 않는다.
- 테이블/컬럼 스키마 점검(`ensureTableExists`)도 시작 시 1회만 수행한다.
- 같은 대상 파일(같은 videoID)에 대한 다운로드는 키 단위 락으로 직렬화한다. 동시에 들어온 두 요청이 같은 영상을 중복 다운로드하지 않게 한다.
- 서버는 기본적으로 `127.0.0.1`에만 바인딩한다. 다른 호스트에 노출해야 하면 설정에서 명시적으로 바꾼다.

## 3. API 계약 (클라이언트 ↔ 서버)

### `GET /process?q=<검색어>` 또는 `?url=<url>`

단일 영상, 검색어, 또는 플레이리스트를 처리한다.

- 단일 영상 응답:
  ```json
  {
    "success": true,
    "metadata": { "id": "...", "title": "...", "channelId": "...", "channelTitle": "...", "thumbnail": "...", "duration": 253 },
    "local_path": "db/채널명/제목.pcm",
    "db_status": "Success"
  }
  ```
- 플레이리스트 응답:
  ```json
  {
    "success": true,
    "list": [ { "id": "...", "title": "...", "channelId": "...", "channelTitle": "...", "thumbnail": "...", "localPath": "...", "duration": 253 } ],
    "total": 10, "success_count": 9, "failed_count": 1
  }
  ```
- 실패: `{ "success": false, "error": "..." }`

### `GET /getrandom?count=<n>` (기본 10, 최대 100)

```json
{ "success": true, "count": 10, "items": [ /* 위 플레이리스트 아이템과 동일한 구조 */ ] }
```

### `GET /deactivate?id=<videoId>`

해당 곡을 추천 대상에서 제외한다.

```json
{ "success": true, "message": "Video deactivated successfully", "id": "..." }
```

> 클라이언트는 `/process`(플레이리스트)의 `list`와 `/getrandom`의 `items`를 각각의 키로 읽는다. 두 엔드포인트의 아이템 스키마(`PlayItem`)는 동일하게 유지한다.

## 4. 데이터 모델

`youtube_videos` 테이블: 곡 하나당 한 행. `id`(YouTube videoId)가 기본키. `activate` 플래그로 추천 목록 포함 여부를 제어한다(삭제 대신 비활성화).

## 5. 보안 / 설정 원칙

- 디스코드 토큰, YouTube API 키, MySQL 자격증명, OAuth 시크릿은 모두 `config.json`(클라이언트/서버 각각)에만 두고, `.gitignore`로 커밋을 막는다. 저장소에는 `config.example.json` 형태의 템플릿만 남긴다.
- 위 비밀값 중 하나라도 외부에 노출된 적이 있다면 코드를 고치기 전에 먼저 재발급한다.
- 서버는 로컬 전용 바인딩을 기본값으로 하고, 인증 없이 외부에 노출하지 않는다.

## 6. 레거시에서 반드시 피할 것 (체크리스트)

이전 구현을 검토하며 실제로 발견했던 문제들이다. 재구현 시 아래를 회귀시키지 않는다.

- [ ] 플레이리스트 판별 변수가 채워지지 않아 기능 자체가 죽어있던 문제 (도달 불가능한 분기)
- [ ] 메타데이터를 이미 가져왔는데 duration 파싱을 누락하고 응답에도 안 담던 문제
- [ ] 요청마다 DB 커넥션을 새로 열고 스키마를 다시 점검하던 비효율
- [ ] 동일 영상 동시 요청에 대한 락 없이 중복 다운로드하던 문제
- [ ] 음성 연결을 끊기 전에 오디오 스레드를 멈추지 않아 생기던 use-after-free 위험
- [ ] `GuildInfo` 이동 생성자가 일부 필드(현재 재생 곡)를 누락하던 문제
- [ ] 셸 문자열 조립 + `popen`으로 외부 프로세스를 실행하던 패턴
- [ ] 비밀값이 평문으로 커밋될 뻔했던 문제 (`.gitignore` 부재)

## 7. 로드맵 (초안)

1. 서버: `/process`(단일 영상) + DB 스키마 + 로컬 바인딩
2. 클라이언트: `!j` `!l` `!p` 기본 재생 파이프라인
3. 재생 큐: `!list` `!skip` `!random` `!remove`
4. 플레이리스트 + `!omakase` 추천
5. 배포/빌드 스크립트 정리, CI
